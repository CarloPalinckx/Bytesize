#include <string>
#include <fstream>
#include <vector>
#include <iostream>
#include <sstream>
#include "Library.h"

#ifndef Translation_H
#define Translation_H

using namespace std;

class Translation {
public:
	Translation(string, vector<Level>);
	string letter;
	string bitset;
private:

};

#endif
